datasource db {
  provider = "postgresql"
  url      = env("DB_CONNECTION_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Test {
    id Int @default(autoincrement()) @id
    title     String  
    text     String?  
}